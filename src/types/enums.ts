export enum LoadingStatus {
  Loading = 'loading',
  Pending = 'pending',
	Error = 'error',
}

export enum SortBy {
  Name = 'name',
  City = 'city',
	OrderDesc = 'desc',
}

export enum SearchParams {
  Sort = 'sort',
  Order = 'order',
	Filter = 'filter',
}

